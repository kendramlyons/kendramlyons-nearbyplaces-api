require('dotenv').config();
const { Pool } = require('pg');
// check end of line 4
// const postgreConnectionString = 'postgresql://${process.env.POSGRES_USER}':${process.env.POSTGRES_PASSWORD}@${process}
//finish lines 6-8
// const postgrePool = new Pool({
//     connectionString
// })